import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import Agenda from './Agenda';

createRoot(document.getElementById('root')).render(<StrictMode><Agenda /></StrictMode>);
